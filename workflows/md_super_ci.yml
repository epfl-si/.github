name: Super CI Template Workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Run tests
        run: echo "Running tests..."
        
      - name: Display Custom Message
        run: echo "${{ env.REPO_CUSTOM_MESSAGE }}"